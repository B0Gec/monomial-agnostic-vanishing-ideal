backend_test.py shows no errors with pytest for test_numpy()!!!
so maybe numpy backend works
Examples:
 - gpu.py probably don't work since backend=torch
 - jax.py probably don't work since backend=torch
 - preprocess.py seems to work without errors.
 - sandbox.ipynb seems to work without errors.
 -

